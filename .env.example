# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (Update with your hosted PostgreSQL details)
DB_HOST=your_hosted_postgres_host
DB_PORT=5432
DB_NAME=your_database_name
DB_USER=postgres
DB_PASSWORD=your_secure_password

# JWT Configuration
JWT_SECRET=your_very_secure_random_secret_key_min_32_chars

# Logging (optional)
LOG_LEVEL=verbose

# CORS Configuration (for production)
# Update in app.js if using custom domain
